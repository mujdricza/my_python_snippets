

#
```
logging.basicConfig()
```

- configures for the whole imported/used modules in general
- has effect to all following
    - root-level logs in the same module
    - non-root-level logs in other modules (thus, root-level logs won't be affected)

# Import

- The order of the imports is relevant for loading the basicConfig() features.

